using UnityEngine;

public class BoardColorChanger : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //get the photo 2D texture from the Photo Camera Manager
        //
        //start in the red channel         
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
